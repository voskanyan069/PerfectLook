<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="am.perfectlook.perfectlook">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.PerfectLook">
        <activity android:name=".activities.ThisShapeActivity"></activity>
        <activity android:name=".choosecalculate.CalculateBodyTypeActivity" />
        <activity android:name=".choosecalculate.ChooseBodyTypeActivity" />
        <activity android:name=".choosecalculate.ChooseCalculateActivity" />
        <activity
            android:name=".authentication.SignupActivity"
            android:windowSoftInputMode="stateVisible|adjustPan" />
        <activity
            android:name=".authentication.LoginActivity"
            android:windowSoftInputMode="stateVisible|adjustPan" />
        <activity android:name=".activities.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>